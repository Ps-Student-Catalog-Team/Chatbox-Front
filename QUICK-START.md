# 🚀 快速开始指南

这个项目可以在 5 分钟内完成本地部署，适合先体验功能或进行二次开发。

---

## 1. 安装 Go 环境

请先安装 Go，并确认命令行中可以正常执行：

```bash
go version
```

如果没有 Go 环境，请先安装 Go 1.20+。

---

## 2. 启动服务

进入项目根目录后执行：

```bash
go run main.go
```

启动成功后，终端会打印当前可访问的地址，例如：

```text
局域网聊天室已就绪，启动于
  127.0.0.1:40001
```

---

## 3. 打开页面

在浏览器中访问：

- 用户聊天页面： http://127.0.0.1:40001/index.html
- 管理后台： http://127.0.0.1:40001/admin.html
- 忘记密码： http://127.0.0.1:40001/forgot-password.html

如果你想从其他设备访问，请把 127.0.0.1 替换为当前机器的局域网 IP。

---

## 4. 默认管理员账号

系统启动后会创建默认管理员密码：

```text
admin666
```

如果你想修改它，可以编辑项目根目录下的 config.txt 文件，并将其中的 adminpassword 项改为你自己的密码。

---

## 5. 常见文件说明

- main.go：服务端入口
- index.html：聊天主界面
- admin.html：管理员后台
- forgot-password.html：密码重置页面
- chat.db：启动后自动生成的数据库文件
- uploads/：上传文件目录

---

## 6. 生成可执行文件

如果你要打包成 Windows 程序，可以执行：

```bash
go build -o chatbox.exe main.go
```

随后直接运行生成的 exe 文件即可。

---

## 7. 使用建议

- 首次使用建议先注册一个用户账号
- 先通过管理员后台确认密码和权限配置
- 如果页面未正常显示，优先检查浏览器控制台和后端启动日志

如果你需要进一步扩展功能，建议先从 main.go 与前端 JS 文件入手。
